import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartjob.agent',
  appName: 'SmartJob Agent',
  webDir: 'dist/trabajo-linkedin/browser'
};

export default config;
